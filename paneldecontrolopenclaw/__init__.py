"""Top-level package for Panel de Control OpenClaw.

This package contains the modules required to run the graphical
interface, proxy server, database, event bus and other supporting
utilities.  It can be installed via pip and provides two console
entrypoints:

* ``paneldecontrolopenclaw``: launches the full application (GUI + proxy).
* ``paneldecontrolopenclaw-proxy``: launches only the proxy service.

Version number is defined in ``pyproject.toml``.
"""

__all__ = []